---
title: Events
---

# Events

One file per event (or event type). Include `source_ingest` (or `sources`) in frontmatter and a **Sources** section. Use stable identifiers; merge when the same event appears in multiple ingest files.
